# tray_ocv2 — 트레이 OCV 위치 구배: 현 상황 진단 · 원인 규명 · 보정 필요성

> 활성화공정 트레이(12×12=144셀) 단위 전압 측정에서 나타나는 **셀 위치에 따른 전압 강하량 차이**를
> ① 현 상황 진단 → ② 원인 규명 → ③ 무엇을 보정해야 하는가 순으로 분석하고,
> **비전공 청중이 이해할 수 있는 발표 자료**를 생성한다.
>
> `tray_ocv` 저장소의 후속. 발표 목적에 맞춰 처음부터 재설계했다.

---

## 문서 (먼저 읽을 것)

| 문서 | 내용 |
|---|---|
| [`docs/00_facts.md`](docs/00_facts.md) | **확정 사실** — 공정·설비·측정계·데이터. 모든 분석의 전제. 새 사실은 여기에만 추가 |
| [`docs/01_analysis_plan.md`](docs/01_analysis_plan.md) | **분석 계획** — A~J 그룹, 각 분석에 ID 부여 (코드·그림이 같은 ID 사용) |
| [`docs/02_presentation_story.md`](docs/02_presentation_story.md) | **발표 시나리오** — 슬라이드 20장 흐름, 용어 치환표 |
| [`docs/03_viz_guidelines.md`](docs/03_viz_guidelines.md) | **시각화 규칙** — R1~R10. `style.py` 가 코드로 강제 |
| [`docs/04_logic_audit.md`](docs/04_logic_audit.md) | **논리 감사** — 계획의 오류 5건과 수정, 추가 분석 6건 |
| [`docs/05_run_log.md`](docs/05_run_log.md) | **실 데이터 실행 로그** — run 결과 원본·해석·액션아이템을 실행마다 누적 기록 |

---

## 핵심 전제 3가지

1. **측정계가 두 개다.** 중간 OCV #01~#07은 **충방전기 내부(팬 가동 중)**, 전용 PRVT OCV #01~#03은
   **별도 OCV 계측기**. → 두 계열의 지문을 **분리해서** 봐야 한다.
2. **`docv7 = PRVT1 − PRVT3` 에는 팬의 직접 경로가 없다.** 팬은 오직 이월(충전 중 온도차 →
   SEI·자기방전율 차이)로만 도달한다. → **팬 서명이 docv7에 있으면 진짜 물리적 차이이고,
   보정 대상이 아니라 공정 개선 대상이다.**
3. **현행 라벨(A/E)은 현행 판정 로직으로 만들어졌다 → 순환 논리.** 라벨 대비 성능은
   "정확도"가 아니라 "현행 로직과의 일치도"다. 비순환 평가는 **spike-in 회수 시험(I3)** 으로 한다.

---

## 진단의 최우선 질문

**자기방전을 이미 3번 재고 있다.**

| 지표 | 정의 | SOC | 구간 |
|---|---|---|---|
| S_A | OCV#02 − OCV#03 | 10 % | 고온에이징1 + 상온2 |
| S_B | OCV#04 − OCV#05 | 70 % | 고온에이징2 + 상온3 |
| S_C | PRVT#01 − PRVT#03 (= docv7) | 30 % | 상온5 + 상온6 |

> **세 지표가 셀 수준에서 서로 상관이 있는가?** (분석 ID `C1`)
> - **있다** → 자기방전은 셀 고유의 재현 특성. 현 판정은 실체를 보고 있고, 셋을 합치면 더 정확해진다.
> - **없다** → docv7은 대부분 구간 특이 잡음. 단일지표 판정 자체가 취약하다.
>
> 그리고 **S_A·S_B는 현행 라벨 생성에 쓰이지 않았다** → 현 E등급 셀이 S_A·S_B에서도
> 튀는지 보면 **분해 없이 순환 논리를 반쯤 깰 수 있다** (`C2`).

---

## 설치 · 실행

```bash
pip install -r requirements.txt

# 1) 합성 데이터로 전체 파이프라인이 도는지 먼저 확인 (실 데이터 없이 가능)
python -m tests.test_pipeline
python -m tests.demo_style          # → reports/figures/demo/*.png

# 2) 실제 파일로: 먼저 컬럼 매핑을 확인
python -m tray_ocv2.cli inspect --input "<Export파일>.xlsx"

# 3) 전체 진단 실행 (A→K→C→F 순, docs/01_analysis_plan.md 실행순서 참고)
python -m tray_ocv2.cli run --input "<Export파일>.xlsx" --outdir reports/run1
# → reports/run1/report_auto.md, figures/*.png, processed_cell_table.csv
```

**실행 환경 제약**: Python 3.8 · 32bit · **scipy/sklearn 없음** → 통계·회귀·이상탐지를
전부 numpy로 직접 구현한다. 한글 폰트(Malgun Gothic)는 사용 가능 → **그림 라벨 전부 한글**.
Windows 콘솔 인코딩(cp949) 문제로 `cli.py` 가 stdout/stderr 을 UTF-8로 강제한다 —
터미널에 글자가 깨져 보이면 실행 전 `chcp 65001` 을 권장(리포트 파일 자체는 정상 UTF-8).

---

## 구조

```
tray_ocv2/
  docs/              00 사실 · 01 분석계획 · 02 발표시나리오 · 03 시각화규칙 · 04 논리감사 · 05 실행로그
  tray_ocv2/
    schema.py         실 Export 컬럼명 ↔ 도메인 개념 매핑. 좌표계·팬기하·판정로직 상수
    io_load.py         파일 로딩 + 셀 단위 표준 테이블 구성 (한글 오전/오후 시각 파서 포함)
    fields.py           격자 변환 · 로버스트 통계 · 트레이 내 편차 (공유 기반)
    qc.py             [A] 신뢰성: LSB·결측·중복·등급구성분해(A7)·단위대조(A9)·시계검증(A8)
    timing.py         [K] 시간축: 공정순서 실측역추정·에이징시간·율변환·휴지시간(K10)·
                          아레니우스등가(오류9)·시간편향(K3)
    indicators.py     [C] 자기방전 3지표: 상관(C1)·신뢰도/감쇠보정(오류8)·꼬리일관성(오류11)·
                          꼬리가속열화판별·준독립대조(C2)
    fan.py              [F] 팬 서명: 세로/밴드별프로파일(F1·F2)·예측열검정(오류6)·군집재현성
                          (오류7)·발열량회귀(오류10)·반경프로파일 등방/이방성(오류12·13)
    spatial.py         [E] 공간지문: 분위수/MAD 필드·PCA(EOF, numpy SVD)·분산분해(E8, 오류5)
    patterns.py        [L] 패턴 유형·비율: 링/역링 자동분류·유형별 구배크기·불량률·
                          고정보정한계·split-half 재현성
    impact.py          [I] 판정영향: 위치별 불량률 배수(I2)·spike-in 비순환 성능지표(I3)
    cli.py            명령행 진입점 (inspect / run)
    style.py          발표용 시각화 기반 (한글폰트·색언어·트레이 히트맵 표준형)
  tests/
    make_synthetic.py 스키마와 일치하는 합성 Export 생성 (팬서명·자기방전·결측 내장)
    test_pipeline.py  전체 파이프라인 스모크 테스트 (77건)
    demo_style.py     스타일 데모
  data/raw/          입력 (git 제외)
  reports/           산출 리포트·그림·처리된 테이블 (git 제외)
```

**구현 상태**: A(신뢰성) · K(시간) · C(3지표) · F(팬 서명, 등방+이방성) · **E(공간지문·
분산분해) · L(패턴유형) · I(판정영향·spike-in)** 까지 코드로 구현·합성 데이터로 검증
완료. B/D/G/H/M 그룹(정보흐름·상관지도·측정계분리·시계열·ML)은 계획 문서에만 있고
미구현(`docs/01_analysis_plan.md` 참고).

### 시각화 규칙 요약 (`style.py` 가 강제)

- **제목 = 결론 문장**, **부제 = 이 그림 읽는 법** — `kind_fig()` 가 둘 다 요구
- **모든 전압 수치에 판정 기준(0.8 mV) 대비 % 병기** — 컬러바 이중 눈금 / `mv_text()`
- 발산형 컬러맵은 **0 중심 대칭 강제**, 무지개 금지, 색맹 안전
- 트레이 히트맵은 **행 A~L · 열 1~12** 라벨, 결측은 해칭, 팬 오버레이 옵션
- **p값 표기 금지** (N≈10만이라 전부 유의 → "유의=중요" 오독 유발). 효과 크기만.

---

## 진행 상태

- [x] 문서 6종 (사실 · 계획 · 발표 시나리오 · 시각화 규칙 · 논리 감사 · 실행 로그)
- [x] `style.py` + 데모 그림 3종
- [x] 좌표계 확정 (알파벳=가로, 숫자=세로=팬밴드축) + 스키마 매핑 (실 컬럼 156개)
- [x] `io_load` 로더 · `schema.verify_position_consistency`(A6) · 한글 오전/오후 시각 파서
- [x] **A(신뢰성)** — LSB(A1)·결측맵(A2)·중복(A5)·시계검증(A8)·**등급구성분해(A7)**·**단위대조(A9)**
- [x] **K(시간)** — 공정순서 실측역추정·에이징시간·율변환(mV/day)·**직전휴지시간(K10, 오류1 검증)**·
      아레니우스등가·**시간편향(K3)**
- [x] **C(3지표)** — S_A/S_B/S_C 계산·**상관행렬(C1)**·**신뢰도·감쇠보정(오류8)**·
      **꼬리일관성(오류11)·꼬리가속열화판별**·**준독립대조(C2)**
- [x] **F(팬 서명)** — 세로/밴드프로파일·예측열검정(오류6)·군집재현성(오류7)·발열량회귀(오류10)·
      **반경프로파일 등방+이방성(오류12·13)**
- [x] **E(공간지문)** — 분위수/MAD 필드·PCA(EOF)·**분산분해(E8, 오류5의 "10%" 정식 대체)**
- [x] **L(패턴 유형·비율)** — 링/역링 자동분류·유형별 구배크기·불량률·고정보정한계·split-half
- [x] **I(판정영향)** — **위치별 불량률 배수(I2)**·**★spike-in 비순환 성능지표(I3)**
- [x] 합성 데이터로 전체 파이프라인 검증 완료(스모크 77건) — 팬패턴·자기방전·핫셀위치까지
      설계값과 정확히 일치 확인(I2가 합성 핫셀 위치를 정확히 짚어냄)
- [x] 실 데이터 Run #1~#3 완료 — 오류1(자기방전 아님)·오류11(희귀사건)·오류12(부호모순) 등
      실측으로 확인, `docs/05_run_log.md` 에 누적 기록
- [ ] B(정보흐름) · D(상관지도) · G/H(측정계분리·시계열) · M(ML 인자투입) · J(보정설계)
      — 계획 문서만 존재 (`docs/01_analysis_plan.md` 참고)
