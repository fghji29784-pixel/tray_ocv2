# 확정 사실 — 공정 · 설비 · 데이터

> 분석의 모든 전제. 새로 확인되는 사실은 여기에만 추가하고, 다른 문서는 이 파일을 참조한다.
> 각 항목에 **확인 상태**를 표기: `확정` / `미확인` / `추정`

---

## 1. 제품 · 공정

| 항목 | 값 | 상태 |
|---|---|---|
| 셀 형태 | 원통형(cylindrical) | 확정 |
| 트레이 | **12행 × 12열 = 144셀**. Cell No 1~144 = A01~L12 | 확정 |
| 랏 규모 | 약 703~1035 트레이 / 랏 (랏별 상이) | 확정 |

### 1.1 공정 순서

```
LCI > RT1 > OCV1 > C1 > C2 > OCV2 > HT1 > RT2 > OCV3 > C3 > C4 > OCV4 > HT2 > RT3 > OCV5
    > C5 > C6 > C7 > OCV6 > D1..D7 > OCV7 > RT4 > PRVT1 > RT5 > PRVT2 > RT6 > PRVT3
```

- `C`=충전, `D`=방전, `RT`=상온 에이징, `HT`=고온 에이징(60°C), `LCI`=Low Current Inspection
- 연속(휴지 없는) 충전 블록: `{C1,C2}` / `{C3,C4}` / `{C5,C6,C7}` · 연속 방전 블록: `{D1…D7}`

### 1.2 각 OCV 측정 시점의 SOC

| 측정 | SOC | 비고 |
|---|---|---|
| OCV1 | ≈0% | 충전 전 |
| OCV2 / OCV3 | 10% | **HT1 전 / 후 — matched-SOC** |
| OCV4 / OCV5 | 70% | **HT2 전 / 후 — matched-SOC** |
| OCV6 | 100% | 만충 |
| OCV7 | 30% | 방전 후 |
| PRVT1 / 2 / 3 | 30% | 전용 OCV 3점 |

---

## 2. 측정계 — **두 개의 서로 다른 시스템** ★

| 측정 | 장소 | 상태 |
|---|---|---|
| **중간 OCV #01~#07** | **충방전기 내부 (in-situ)** — 팬 가동 중 | **확정** |
| **전용 PRVT OCV #01~#03** | **별도 OCV 계측기** — 충방전기 밖 | **확정** |

**이 분리가 분석 설계의 핵심이다.**

- 중간 OCV 계열에는 **충방전기(팬 기류)의 지문**이 찍힌다.
- PRVT 계열에는 **OCV 계측기(지그·프로브·채널·주변 열환경)의 지문**이 찍힌다.
- `docv7 = PRVT1 − PRVT3` 이므로 **팬은 docv7에 직접 각인되지 않는다.**
  팬 → docv7 경로는 오직 **이월**뿐:
  `충전 중 기류 불균일 → 위치별 셀 온도차 → SEI·자기방전율 차이 → docv7`
  → **팬 서명이 docv7에서 발견되면 측정 아티팩트가 아니라 진짜 물리적 차이이며,
    보정 대상이 아니라 공정 개선 대상이다.**

### 2.1 데이터 확보 상태

| # | 항목 | 상태 | 의미 |
|---|---|---|---|
| **Q4** | **중간 OCV #01~#07의 셀 온도** | **확보 가능 (확정)** ★★ | **충방전기 내부 온도 = 팬 기류장의 직접 측정치.** 아래 §2.2 |
| **Q5** | **측정 시작 시각 (타임스탬프)** | **확보 가능 (확정)** ★★ | **구간 실제 경과시간 → 자기방전을 율(mV/day)로 환산 가능.** 아래 §2.3 |
| Q1 | OCV 계측기: 일괄 측정 vs 셀별 스캔 | 미확인 | §2.4 가정으로 일단 배제 |
| Q2 | OCV 계측기 대수 | 미확인 | 〃 |
| Q3 | 충방전기→계측기 이동 시 위치 보존 여부 | 미확인 | 〃 |
| Q6 | 트레이 ID의 랏 간 재사용 | 미확인 | 〃 |

### 2.2 ★ 중간 OCV 온도가 여는 것

중간 OCV는 **팬이 돌아가는 충방전기 안에서 측정**되고, 이제 **그 순간의 셀 온도**가 있다.

1. **팬 기류장의 직접 측정.** 전압은 SOC·용량·dU/dT가 섞이지만 **온도는 순수한 열장**이다.
   → 팬 서명 검증(F 그룹)을 **전압이 아니라 온도로 직접** 수행할 수 있다. 훨씬 깨끗하다.
2. **팬 기류장을 7번 독립 측정한 셈** (OCV#01~#07). **7장의 온도 필드가 서로 재현되면
   그 자체가 팬 가설의 증거**이고, 평균하면 SNR이 √7 개선된다.
3. **dU/dT 실측 가능.** 같은 셀·같은 SOC에서 온도와 전압을 동시에 얻으므로,
   트레이 내 회귀로 **SOC 단계별 dU/dT를 데이터에서 직접** 뽑을 수 있다.
4. **온도 보정 잔차 OCV.** 각 단계 전압에서 온도 몫을 제거한 "순수 전기화학" 성분을 분리
   → **단계별로 "구배 중 온도 몫 vs 비온도 몫"을 정량화**할 수 있다.
5. **S_A·S_B의 오염 제거.** `S_A = OCV2 − OCV3` 은 두 측정 시점의 온도가 다르면
   `dU/dT × ΔT` 만큼 오염된다. 이제 보정 가능 → **순수 자기방전 지표**가 된다.

### 2.3 ★ 타임스탬프가 여는 것

1. **구간 실제 경과시간 확보.** 지금까지 "간격 ≈1일, ≈2일"은 **추정값**이었다.
   실측 Δt를 쓰면 자기방전 지표를 **mV → mV/day (율)** 로 환산할 수 있다.
2. **세 지표의 통일 비교 가능.** S_A·S_B·S_C는 구간 길이·온도가 전부 다르다.
   시간·온도 정규화(아레니우스) 후에야 **등가 누설전류로 같은 자에 놓고 비교**할 수 있다.
3. **라인 정체 = 숨은 판정 편향.** 트레이마다 에이징 실제 시간이 다르면 전압 강하량도
   비례해 달라진다. 트레이별 mode 판정이 일부 상쇄하지만 완전하지 않다.
   → **"에이징 시간 편차 → 판정 편차"** 는 지금까지 아무도 확인하지 않은 축이다.
4. **시계열 진단(H 그룹) 가능.** 트레이 처리 순서 축이 생긴다.
5. 셀별 타임스탬프까지 있으면 **트레이 내 측정 순서(스캔) 효과**도 검사 가능.

### 2.3b 데이터 요청 목록 (우선순위)

> **원칙: "모든 스텝의 시작/종료 시각"으로 한 번에 받는다.** 나중에 재요청하는 비용이
> 지금 한 번에 받는 비용보다 훨씬 크다.

| 순위 | 항목 | 왜 필요한가 | 상태 |
|---|---|---|---|
| **1** | **에이징 스텝(HT1·HT2·RT1~RT6) 시작/종료 시각** ★★ | 자기방전은 에이징 중에 일어난다. **60°C 자기방전은 25°C의 10~30배.** OCV 측정 시각만으로는 총 Δt만 알 뿐 **그중 몇 시간이 60°C였는지 모른다.** 총 Δt가 같아도 HT가 길었던 트레이는 자기방전이 훨씬 크다 → `K3`·`K5`가 이것 없이는 반쪽 | 요청 필요 |
| **2** | **충·방전 스텝 시작/종료 시각** ★ | ① **CV가 지운 정보 복원**(§2.3c) ② **위치별 냉각 시상수 τ**(`F6`) ③ 열 노출 적분 | 요청 필요 |
| 3 | OCV 측정 시각 | 구간 Δt, 율(mV/day) 환산 | **확보** |
| 4 | 중간 OCV 셀 온도 | 팬 기류장 직접 측정 | **확보** |

> ⚠️ **충방전 시각으로는 HT1을 가를 수 없다.** 공정 순서가 `OCV2 > HT1 > RT2 > OCV3` 라
> 그 사이에 충방전 스텝이 없다. **에이징 스텝 자체의 시각이 따로 필요하다.**

**같이 확인할 것**: 시각이 **셀 단위인가 트레이 단위인가.**
충전은 채널별로 돌므로 셀 단위일 가능성이 높고, 그렇다면 §2.3c의 저항·용량 프록시가
**셀별**로 생겨 가치가 훨씬 크다. 트레이 단위면 τ·열노출은 되지만 셀별 프록시는 불가.

### 2.3c ★ 스텝 소요시간이 여는 것

**(1) CV가 지운 정보를 시간이 복원한다**

| 스텝 종료 조건 | 종료전압 | 소요시간 |
|---|---|---|
| CC 종료 (시간 기준) | **정보 있음** | 고정 → 정보 없음 |
| **CV 종료 (전압 기준)** | 강제 → **정보 없음** | **정보 있음** ★ |

원칙: **고정된 쪽은 버리고 가변인 쪽을 취한다.** 두 변수는 상보적이다.
정전압 종료 스텝을 분석에서 제외했던 이유(전압 강제)가 **소요시간으로는 해소**된다.
또한 **CV 구간 길이는 셀의 저항·용량 상태를 직접 반영**한다(저항↑ → CV↑, 용량↑ → CC↑)
→ DCIR 컬럼 없이 저항 지표를 하나 더 얻는다 (`D7`의 분극 프록시와 상보).

**(2) 위치별 냉각 시상수 τ(pos)** — 팬 가설의 직접 측정
온도만으로는 "지금 몇 도"까지다. **온도 + 시간이 있어야 "얼마나 빨리 식는가"** 를 얻는다.
팬 가설의 직접 예측: **팬 축 아래는 τ가 짧고, 팬 사이·배열 바깥은 τ가 길다.**
온도 스냅샷 비교(`F2`)보다 한 단계 강한 증거 — "온도가 다르다"가 아니라
**"열이 빠져나가는 속도 자체가 위치마다 다르다"** 를 보인다.

**(3) 열 노출 적분** — 아레니우스 열 이력은 온도 × 시간. duration 없이는 적분 불가.

### 2.3d 컬럼 인벤토리 (실제 Export 확인 — 2026-07)

**확보 완료**

| 그룹 | 컬럼 | 비고 |
|---|---|---|
| LCI #01~#02 | 시작시간 · 종료시간 · 작업시간 · **온도** | ★ **발열 전 대조군** — `F3` 검증의 baseline |
| OCV #01~#07 | `OCV #0X OCV` · 시작시간(일부 종료·작업) · **`OCV #0X_온도`** | ★ 충방전기 내부 온도 = 팬 기류장 |
| Charge #01~#07 | 시작·종료·작업시간 · 최저/평균/최고 온도 | |
| DisCharge #01~#07 | 시작·종료·작업시간 · 최저/평균/최고 온도 | |
| PRIVT OCV #01~#03 | OCV · 시작시간 · 온도 | |
| Delta OCV #07 | `Delta OCV #07 DOCV` | = docv7 (단위 확인 필요) |
| **에이징 시각** | `Start/End Time_High Temp Aging #01~#02`<br>`Start/End Time_Room Temp Aging #01~#09` | ★★ **요청 1순위 확보** → K 그룹 전부 개방 |
| 종료전압 | `End Voltage_Charge #01~#02`<br>`End Voltage_DisCharge #01~#03` | ⚠️ 일부만 (§아래) |
| **설비 ID** | **`Work BOX_High Temp Aging #01~#02`** | ★ **고온에이징 챔버 ID로 추정** — 정체 확인 필요 |

**★ 새로 드러난 사실 2가지**

1. **`Work BOX_High Temp Aging` = 설비 메타데이터일 가능성.**
   그동안 "어느 설비인지 특정할 메타데이터가 없다"가 최대 난관이었다.
   챔버 ID가 맞다면 **`H3`(지문 군집화로 설비 역추정)을 추정이 아니라 실측으로 대체**할 수 있다.
   → **이 컬럼의 정체 확인 필요** (챔버 호기? 박스 번호? 슬롯?)

2. **상온 에이징이 9개(RT #01~#09)다.** 기존 문서의 공정 순서(RT1~RT6)와 다르다.
   → **`docv7 = PRVT1 − PRVT3` 구간이 RT #01~09 중 어느 번호인지 매핑 확인 필수.**
   틀리면 `K2`(율 환산)·`K5`(아레니우스)가 통째로 틀린다. 시각으로 역추적 가능.

**추가 요청 권장 (우선순위)**

| 순위 | 항목 | 왜 |
|---|---|---|
| **1** | **TRAY ID · Cell No(또는 Cell 위치) · Label** | 목록에 안 보임. **없으면 분석 자체가 불가.** 확인만 |
| **2** | **`End Voltage_DisCharge #07`** ★ | 현재 #01~#03만. **#07이 있어야 OCV#07과 짝지어 분극(저항 프록시) 계산 가능**(`D7`). 방전 직후 분극이 가장 커 저항 신호가 제일 선명 |
| **3** | 충전/방전 **용량(Ah)** | SOC 정규화(미해결) 직접 가능 + 셀 물성 지표 |
| **4** | **충방전기 호기 ID** | 팬 배열은 동일해도 **호기별 팬 열화·필터 막힘** 차이 확인용 |

> 나머지 End Voltage(Charge #03~07, DisCharge #04~06)는 **CV 종료면 전압이 강제되어 정보가
> 없으므로 불필요**하다. 그 스텝들은 **작업시간**이 정보를 갖고 있고 이미 확보됐다(`K9`).

**식별·이력 컬럼 (추가 확인됨)**

`공정경로` · `Product` · `Lot` · `Tray ID` · `Cell ID` · `Can ID` · `Vent ID` ·
`등급` · `샘플링 이전 등급` · `Cell No` · `Cell 위치`

사용 방침 (확정):

| 컬럼 | 용도 | 상태 |
|---|---|---|
| `Tray ID` · `Cell No` · `Cell 위치` | **위치 파싱의 근간** (12×12, A01~L12) | 필수 |
| `등급` | 판정 라벨 (A=양품 / E=불량) | 필수 |
| `샘플링 이전 등급` | — | **무시 (사용자 지시)** |
| `Cell ID` | 셀 식별자. **제조 순번과 무관** → 상류 생산 순서 검증에는 쓸 수 없음 | 식별용만 |
| `Can ID` · `Vent ID` | 부품 로트라면 **로트별 층화** → 불량 편중 확인 (부가) | 중 |
| `공정경로` · `Product` · `Lot` | 층화·교란 통제 변수. 랏은 1개 | 중 |

> ⚠️ **라벨 순환은 여전히 깨지지 않았다.** `Cell ID`(순번 무관)와 `샘플링 이전 등급`(무시)
> 모두 순환 밖 정보를 주지 못한다. 따라서 **비순환 검증은 전적으로 아래 둘에 의존한다**:
> - **`I3` spike-in 회수 시험** (인공 결함 주입 → 위치별 검출력)
> - **`C2` S_A·S_B 준독립 대조** (라벨 생성에 쓰이지 않은 지표)
>
> → **`C2`의 중요도가 한 단계 더 올라간다.** 실질적으로 유일한 "실제 데이터 기반" 교차검증이다.

**상온 에이징 범위 (확정)**: **RT #01~#06 만 사용**, #07 이후는 무시.
지표 계산에 지장 없음 — 아레니우스에 필요한 건 **60 °C 노출시간(HT #01/#02)** 이고,
RT 구간은 "전체 Δt − HT"로 구한다. **`S_C`(docv7)의 Δt는 PRVT 시작시간으로 직접** 얻는다.
RT #01~#06은 리드타임 관리도(`K7`)에만 사용.

**데이터 수령 후 확인할 것**

- 시각·온도가 **셀 단위인가 트레이 단위인가** → 셀 단위면 `K6`(트레이 내 스캔 순서)까지 개방
- 시각 포맷·시간대·결측
- `Delta OCV #07 DOCV`(mV 추정) vs `PRIVT OCV`(V 추정) **단위 불일치** — 구 저장소에서
  `unit_scale_to_mv=1000`, `docv7_unit_scale_to_mv=1` 이었음
- RT/HT 번호 ↔ 공정 순서 매핑

### 2.4 현재 적용 중인 가정 (⚠️ 잠정)

> **가정 A1**: OCV 계측기(장비)의 개체차·채널차는 **없다**고 본다.
> **가정 A2**: 트레이(캐리어) 개체차는 **없다**고 본다.
>
> 이 가정 하에서 **G 그룹(측정계 분리)·H4(트레이 재현성)는 보류**한다.
> 단 가정일 뿐이므로, 다른 원인으로 설명되지 않는 잔차가 남으면 **가장 먼저 되살릴 후보**다.
> 특히 `docv7`은 전적으로 PRVT 계열에서 나오므로, 계측기 가정이 틀리면 결론이 바뀐다.

---

## 3. 충방전기 설비

| 항목 | 값 | 상태 |
|---|---|---|
| 충방전기 1대당 트레이 | **1장 (144셀)** | 확정 |
| 냉각팬 | **4 − 5 − 4 = 13개, 3밴드 엇배열(staggered)** | 확정 |
| 팬 가동 | **상시 가동** | 확정 |
| 라인 내 호기별 팬 배열 | **전 호기 동일** | 확정 |
| 팬 직경 | **미확정 (보류)** | 미확인 |

### 3.0 ★ 좌표계 방향 — **확정** (2026-07 도면 주석)

```
        A  B  C  D  E  F  G  H  I  J  K  L     ← 알파벳 = 가로 = 열
     1 ┌────────────────────────────────┐
     2 │    ○      ○      ○      ○      │   팬 상단 밴드 4개
     3 │                                │
     4 ├────────────────────────────────┤
     5 │   ○    ○    ○    ○    ○        │   팬 중앙 밴드 5개
     6 │                                │
     7 │                                │
     8 ├────────────────────────────────┤
     9 │    ○      ○      ○      ○      │   팬 하단 밴드 4개
    10 │                                │
    11 │                                │
    12 └────────────────────────────────┘
     ↑ 숫자 = 세로 = 행 = **팬 3밴드가 쌓인 축**
```

- **알파벳 A~L = 열(가로)** · **숫자 1~12 = 행(세로)**
- **팬 3밴드는 숫자 축(세로)** 을 따라 쌓이고, 밴드 내 4-5-4 배열은 **알파벳 축(가로)**
- `Cell No` 규칙: `col = (n−1)//12+1` (A~L), `row = (n−1)%12+1` (1~12)
  → **A01→A02→…→A12→B01** 순으로 **세로로 먼저** 증가 (실데이터에서 `A6` 로 검증)

> ⚠️ **12×12 정사각이라 축을 뒤집어도 에러가 나지 않는다.** 전치된 채 조용히 계산되고,
> 팬 배열이 행/열 비대칭(3밴드 vs 4-5-4)이라 **`F1`·`F2` 결론이 정반대**가 된다.
> ※ **구 저장소 `tray_ocv` 문서에는 "알파벳=행"으로 잘못 적혀 있다.** 그대로 믿지 말 것.

코드 반영: `schema.ALPHABET_AXIS = "col"`, `schema.FAN_BAND_AXIS = "row"`

### 3.1 팬 중심 좌표 (셀 격자 기준, 도면 픽셀 판독 — CAD 검증 필요)

| 밴드 | 세로(행) 중심 | 가로(열) 중심 | 가로 피치 |
|---|---|---|---|
| 상단 (4개) | **2.6** | 1.9 / 5.2 / 8.5 / 11.5 | **3.2 셀** |
| 중앙 (5개) | **6.4** | 2.0 / 4.3 / 6.6 / 8.9 / 11.2 | **2.3 셀** |
| 하단 (4개) | **10.2** | 1.9 / 5.1 / 8.4 / 11.4 | **3.2 셀** |

- 세로 방향 팬 피치 ≈ **3.8 셀**
- **가로 피치가 밴드마다 다르다(3.2 / 2.3 / 3.2) = 비분리형(non-separable) 서명.**
  배선 하네스·MUX 뱅크·트레이 리브 등 경쟁 가설은 전부 분리형이라 이 서명을 만들 수 없다.
- **세로 1·12번 줄이 팬 배열 바깥**에 놓인다 → "테두리 高" 링이 기하만으로 설명될 수 있다.

### 3.2 팬 서명 예측 — **데이터 라벨로 표현** (직경 무관)

| 검증 | 축 | 예측 |
|---|---|---|
| **`F1`** | 세로 (1~12) | **극대(차가움) = 3, 6, 10** / **극소(뜨거움) = 1, 4, 5, 8, 12** |
| **`F2`** ★ | 가로 (A~L) | 팬 중심에 가장 가까운 열이 냉각 최강(차가움). 상·하단 밴드 ≈ **B, E, H, L**<br>중앙 밴드 ≈ **B, D, G, I, K** (`col_centers`에서 반올림 계산, `schema._fan_center_columns()`) |

> **`F2` 판별 문장**: *"세로 1~4 밴드와 9~12 밴드의 가로 프로파일은 서로 같고,
> 5~8 밴드만 주기가 다르다"* → **팬 확정**. 셋 다 같으면 **팬 기각**(분리형 = 배선·기구물).
> ⚠️ 이 상관 기반 판정만으로는 팬을 고유 식별하지 못한다(`docs/04_logic_audit.md` 오류6) —
> 위 예측열 자체를 detrend 잔차로 직접 검정하는 `predicted_minima_check()` 가 판결문 역할을 한다.

> **★수정 이력(2026-07-29)**: 이전 버전의 열 예측(`D, G, J` / `C, E, H, J`)은 팬 **사이(gap)**
> 열을 손으로 잘못 계산해 "냉각 최강"으로 라벨링한 것이었다(행 축 `FAN_ROW_MAX`="팬 중심=
> 차가움" 규약과 불일치). 합성 데이터(정답을 아는 검증)에서 `predicted_minima_check` 가
> 세 밴드 전부 반대로 나와 발견 — `_fan_center_columns()` 로 계산식 기반 교체 후 재검증
> 통과. 상세: `docs/04_logic_audit.md` 2차 감사 오류6.

코드 반영: `schema.FAN_BANDS`, `FAN_ROW_MAX/MIN`, `FAN_COL_COLD_OUTER/MIDDLE`,
`STAGE_THERMAL_STATE`(오류7)

---

## 4. 판정 로직 (현행)

```
docv7 = PRVT1 − PRVT3   (또는 Delta OCV #07 컬럼)
트레이별 docv7 의 mode 산출 → (셀 docv7 − tray mode) > 0.8 mV → 불량(E)
```

- **★Label(A=양품/E=불량)은 이 로직으로 만들어진 것 → 순환 논리.**
  라벨 대비 성능은 "진짜 정확도"가 아니라 "현행 로직과의 일치도"일 뿐이다.
- mode는 값 양자화 때문에 불안정 (타 랏에서 표본 소폭 변경만으로 판정 16셀 뒤집힘 실측).

---

## 5. 기존 랏(N32S D29) 실측 참고값

| 항목 | 값 |
|---|---|
| ΔOCV(=OCV1−OCV3) median / mode | 1.36 / 1.28 mV |
| 판정 임계 (mode+0.8) | ≈ 2.08 mV |
| 불량률 | ≈ 0.09 % (93/99,293셀) |
| docv7 공통 지문 진폭 | **± 0.048 mV** (전 트레이 평균 필드) |
| 트레이별 docv7 위치편차 p2p | **median 0.286 mV** = 판정 오프셋 0.8 mV의 **36 %** |
| 불량 온도핀 비율 | ≈ 2.84 % (트레이당 ~4개) |
| 온도가 ΔOCV를 설명하는 비율 | R² ≤ 0.094 (전용OCV 측정시점 온도 기준) |

> **주의**: 마지막 항목은 **전용OCV 측정시점 온도**로 계산한 것이다. 중간 OCV가 팬 가동 중
> 충방전기 내부에서 측정된다는 사실이 확인된 지금, **중간 OCV 단계의 온도 경로는 아직
> 평가된 적이 없다.** 재평가 필요.

---

## 6. 실행 환경

| 항목 | 값 |
|---|---|
| 설계 기준 PC | Python 3.8.5 · 32bit · numpy 1.24 / pandas 2.0 / matplotlib 3.7 |
| **코드 정책** | **scipy · sklearn 미사용** → 통계·회귀·이상탐지를 전부 numpy로 직접 구현.
  32bit 제약 때문에 시작한 선택이지만, **어느 환경에서 돌리든 유지**한다 (의존성 최소화) |
| 한글 폰트 | **Malgun Gothic · NanumGothic · Noto Sans KR 사용 가능** → 그림 라벨 전부 한글 |

> ⚠️ **실행 PC는 사람마다 다를 수 있다.** 예: 다운로드해 실행하는 PC가 Python 3.14 64bit인 경우,
> `requirements.txt` 의 `numpy<2` 같은 상한이 있으면 그 파이썬 버전용 사전빌드 wheel이 없어
> **소스 빌드를 시도하다 컴파일러 부재로 실패**한다(2026-07 실사례). 버전 상한은 실제로
> 필요한 게 아니면 걸지 않는다 — 하한만 지정하고 pip가 알아서 호환 버전을 고르게 둔다.
